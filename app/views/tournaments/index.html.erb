<h2>Tournaments</h2>

<% @tournaments.each_with_index do |tournament, index| %>
  <p><%= index + 1 %>. <%= link_to "#{tournament.title}", tournament_path(tournament) %></p>
<% end %>

<p><%= link_to "Add A Tournament", new_user_tournament_path(current_user) %></p>
